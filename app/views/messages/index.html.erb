<BR />
<div id="mapcontainer"> 

  <h2>Messages</h2> 

  Received Messages <i>(click to open)</i>

  <div id="accordion">
    <% if @rcvddesc != nil %>
    <% @rcvddesc.each do |a| %> 
    <h3 id="acc"><%= a.subject %></h3>
      <div STYLE="font-weight: bold; margin-left: 30px; background-color: white; width: 45%; padding: 8px;">
      Sent from <%= User.find(a.sender_id).name %> <hr />
      <p  STYLE="font-weight: normal; margin-left: 30px; background-color: white; width: 90%; padding: 8px;"><%= a.body %>
      <p STYLE="text-align: right; padding-right: 20px;"><i>Reply</i></p> </p>
      </div>
      <% end %> 
    <% end %> 
  </div>
<BR><BR>
  <hr />
  Sent Messages <i>(click to open)</i>
  <div id="accordion2">
  <% @sentdesc.each do |a| %> 
  <h3 id="acc"><%= a.subject %></h3>
  <div STYLE="font-weight: bold; margin-left: 30px; background-color: white; width: 45%; padding: 8px;">
  Sent to <%= link_to User.find(a.recipient_id).name, User.find(a.recipient_id) %></p> 
  <div style="clear: both;"></div>
  <hr />
  <p  STYLE="font-weight: normal; margin-left: 30px; background-color: white; width: 90%; padding: 8px;"><%= a.body %>
  <p STYLE="text-align: right; padding-right: 20px;"><i>Reply</i></p> </p>
  </p>

  </div>
  <% end %> 
  </div>


</div>