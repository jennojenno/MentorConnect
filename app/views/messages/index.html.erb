<BR />
<div id="mapcontainer"> 

  <h2>Messages</h2> 
<% if @rcvddesc.count == 0 %>
  You have no received messages. Why not send some to a few <%= link_to "mentors", mentors_path %>? 
<% else %> 
  Received Messages <i>(click to open)</i>
  <div id="accordion">
    <% @rcvddesc.each do |a| %> 
    <h3 id="acc"><%= a.subject %></h3>
      <div STYLE="font-weight: bold; margin-left: 30px; background-color: white; width: 45%; padding: 8px;">
      Sent from <%= link_to User.find(a.sender_id).name, User.find(a.sender_id) %> <hr />
      <p  STYLE="font-weight: normal; margin-left: 30px; background-color: white; width: 90%; padding: 8px;"><%= a.body %>
      <p STYLE="text-align: right; padding-right: 20px;"><i><%= link_to "Reply", new_user_message_path(user_id: current_user, instructor_id: User.find(a.sender_id)) %></i></p> </p>
      </div>
    <% end %> 
<% end %> 
</div>

<BR><BR>
  <hr />
  <BR><BR>
  <div id="accordion2">
    Sent Messages <i>(click to open)</i>
    <BR>
  <% @sentdesc.each do |a| %> 
  <h3 id="acc"><%= a.subject %></h3>
  <div STYLE="font-weight: bold; margin-left: 30px; background-color: white; width: 45%; padding: 8px;">
  Sent to <%= link_to User.find(a.recipient_id).name, User.find(a.recipient_id) %></p> 
  <div style="clear: both;"></div>
  <hr />
  <p  STYLE="font-weight: normal; margin-left: 30px; background-color: white; width: 90%; padding: 8px;"><%= a.body %>
  <p STYLE="text-align: right; padding-right: 20px;"><i><%= link_to "Reply", new_user_message_path(user_id: current_user, instructor_id: User.find(a.recipient_id)) %></i></p> </p>
  </p></div>
    <% end %> 

  </div>


</div>